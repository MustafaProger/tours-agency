# 🏎️ SuperCar Experience

Неоновая платформа для продажи суперкар-впечатлений: закрытые треки, ночные городские прогоны и hypercar-наставники. Проект построен по той же архитектуре, что и Dental College/SoutSide, но с полностью новым визуалом и предметной областью.

## ⚙️ Стек
- **Frontend**: React 18 + TypeScript + Vite + Tailwind (кастомные неоновые темы)
- **Animations**: Framer Motion
- **Icons**: Lucide React
- **Backend**: Node.js (native http) + `@neondatabase/serverless`
- **Database**: Neon PostgreSQL (таблицы `disciplines`, `drivers`, `experiences`, `thrill_reviews`, `bookings`)

## ✨ Ключевые блоки
- Герой-блок с телеметрией и CTA "Initiate session"
- Секция **Pilots** — карточки гонщиков (био, hero car, сертификаты)
- Секция **Programs** — карточки программ (интенсивность, трек, продолжительность)
- Секция **Client telemetry** — отзывы
- Модальное окно бронирования со стилем cockpit (валидация, предложения пилота/программы)
- Нативный API с CRUD для драйверов, программ, отзывов и бронирований

## 🚀 Запуск
```bash
cd SuperCar
npm install
cp env.example .env   # добавьте DATABASE_URL и ADMIN_TOKEN (опционально)
npm run db:init        # применит database-schema.sql (структура + сиды)
npm run server         # backend на http://localhost:3001
npm run dev            # frontend на http://localhost:5173
```
Или `npm run start` (dev server + backend одновременно).

## 🔌 API endpoints
Публичные:
- `GET /health`
- `GET /drivers`
- `GET /experiences?limit=20`
- `GET /reviews?limit=6`
- `GET /bookings`
- `POST /bookings` — поля:
  ```json
  {
    "client_name": "string",
    "client_email": "string",
    "client_phone": "string",
    "driver_id": 1 | null,
    "experience_id": 2 | null,
    "preferred_track_date": "YYYY-MM-DD",
    "preferred_track_time": "HH:mm",
    "participants_count": 1,
    "notes": "string"
  }
  ```

Админ (заголовок `Authorization: Bearer ADMIN_TOKEN`):
- `POST /drivers`, `PUT /drivers/:id`, `DELETE /drivers/:id`
- `POST /experiences`, `PUT /experiences/:id`, `DELETE /experiences/:id`
- `PUT /reviews/approve/:id`
- `PUT /bookings/:id`, `DELETE /bookings/:id`

## 🗄️ Структура БД
`database-schema.sql` создаёт и наполняет:
- `disciplines` — справочник направлений (Hypercar / Track day / GT endurance)
- `drivers` — пилоты/инструкторы (hero car, сертификаты, контакты)
- `experiences` — программы (интенсивность, продолжительность, цена, изображение)
- `thrill_reviews` — отзывы
- `bookings` — заявки (track date/time + nullable driver/experience)

## 🖌 Дизайн
- Градиентные блоки, стеклянные панели, кастомные кнопки с трекингом в 0.4em
- Палитра: #030308, фуксия → оранжевый → янтарный, голубые и салатовые акценты
- Отдельные шрифтовые интерлиньяжи, шумовые паттерны, трековая телеметрия в hero

## ✅ Что сделать, чтобы БД/сервер работали так же
1. Создайте базу в Neon и добавьте URL в `.env`
2. Выполните `npm run db:init` (или `psql "$DATABASE_URL" -f database-schema.sql`)
3. Запустите `npm run server` — сервер будет обслуживать те же маршруты, что и предыдущие проекты, только с новыми таблицами
4. При необходимости наполните таблицы своими данными (см. секцию "Insert" в `database-schema.sql`)

Проект полностью готов к деплою и повторяет архитектуру прошлых работ, но с новым UX/UI и контентом из мира SuperCar.
